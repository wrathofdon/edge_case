// This is an easy place where users can modify settings without having to touch the rest of the code

var globalSettings = {
  // This defines the default behavior when loading a new card.
  // Set true if you want new cards to attach to previous ones
  // Set false if you only want one card on display at a time
  loadAttachDefault: true,
  // If loadAttachDefault is set to true, what happens if the same card is
  // loaded more than once?  Set true if you want to rewind to and reload the
  // previous version.  Set false if you want to attach another copy
  rewindAttachment: true
}
